import { SyllabusCreationDto } from './subject-creation-dto';

describe('SyllabusCreationDto', () => {
  it('should create an instance', () => {
    expect(new SyllabusCreationDto()).toBeTruthy();
  });
});
